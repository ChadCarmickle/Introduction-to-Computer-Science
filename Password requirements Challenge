/* 
Chad Carmickle 
11-13/25 

A website requires that passwords only contain letters or numbers. For each character in passwdStr that is not a letter or number, assign the character with '0' (the number zero).

Click for example
 Ex: If the input is 4  36, then the output is:
Valid password: 40036

Note:

isalpha() returns true if a character is a letter, and false otherwise. Ex: isalpha('a') returns true. isalpha('8') returns false.
isdigit() returns true if a character is a digit, and false otherwise. Ex: isdigit('8') returns true. isdigit('a') returns false. */ 


#include <iostream>
#include <string>
#include <cctype>
using namespace std;

int main() {
   string passwdStr;
   unsigned int i;
   
   getline(cin, passwdStr);
 

   for (i = 0; i<passwdStr.length(); i++){ 
      
      if (isalpha(passwdStr[i])) { 
         
      } 
      else { 
         if (isdigit(passwdStr[i])) { 
             
        } else { 
            passwdStr[i] = '0';   
        } 
      }
}
   

   cout << "Valid password: " << passwdStr << endl;
   
   return 0;
}
